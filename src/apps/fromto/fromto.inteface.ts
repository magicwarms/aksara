export interface queryFindFromtoData {
    isActive: boolean;
    category: string;
}

export interface fromtoName {
    us: string;
    id: string;
}

export interface categories {
    id: string;
}
