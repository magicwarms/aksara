export interface resultCompletion {
    completion: string;
}

export interface featureCompletion {
    feature: string;
    featureKey: string;
    featureCategory: string;
    featureCategoryKey: string;
    featureSubCategory: string;
    featureSubCategoryKey: string;
}

export interface basicDataCompletion {
    key: string;
    name: string;
}
